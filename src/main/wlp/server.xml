<server>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080">
    </httpEndpoint>

     <application name="openliberty" context-root="/" location="${appLocation}" type="war"/>

    <webContainer deferServletLoad="false"/>

</server>
